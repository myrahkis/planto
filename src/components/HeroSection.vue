<script setup></script>

<template>
  <section class="hero-section">
    <div>Info</div>
    <div class="">feedback</div>
    <div>plants cards</div>
    <div class="heading-corners">Out trendy plants</div>
    <div>trendy plant 1</div>
    <div>trendy plant 2</div>
  </section>
</template>

<style scoped>
.hero-section {
  /* margin-top: 10%; */
}
/* .card {
  padding: 2rem;
  margin: 2rem;

  position: relative;
  border-radius: 3rem;
  width: fit-content;
  width: 15rem;
  height: 20rem;
  background-color: var(--blur-bg-color);
  backdrop-filter: blur(2px);
}
.card::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: 3rem;
  padding: 1px;

  background:
    linear-gradient(135deg, rgba(255, 255, 255, 0.3) 0%, transparent 25%) top left / 100% 100%
      no-repeat,
    linear-gradient(315deg, rgba(255, 255, 255, 0.3) 0%, transparent 25%) bottom right / 100% 100%
      no-repeat;

  -webkit-mask:
    linear-gradient(#fff 0 0) content-box,
    linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
} */
</style>
